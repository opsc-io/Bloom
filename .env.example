# Application URL
APP_URL=http://localhost:3000

# NextAuth / JWT secret
NEXTAUTH_SECRET=change-me-super-secret

# Database (Postgres) URL
DATABASE_URL=postgres://user:password@localhost:5432/opsc_db

# Redis (optional, for WebAuthn challenges, rate-limiter, token storage)
REDIS_URL=redis://localhost:6379

# SMTP (for sending emails)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=smtp-user
SMTP_PASS=smtp-password
SMTP_FROM="OPSC <no-reply@example.com>"

# OAuth providers
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
FACEBOOK_CLIENT_ID=
FACEBOOK_CLIENT_SECRET=

# Other
PASSWORD_RESET_TTL=3600
# Example environment variables for OPSC
# Copy this to .env and adjust values for your environment

# App
RP_ID=localhost
ORIGIN=http://localhost:3000
NODE_ENV=development

# Database (Postgres)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASS=postgres
DB_NAME=therapy_platform

# Redis (for sessions/challenges)
REDIS_HOST=localhost
REDIS_PORT=6379

# Secrets
#JWT_SECRET=replace_this_with_a_secure_random_value
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=replace_this_with_a_secure_random_value

# OAuth (Google/Facebook)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
FACEBOOK_CLIENT_ID=
FACEBOOK_CLIENT_SECRET=
